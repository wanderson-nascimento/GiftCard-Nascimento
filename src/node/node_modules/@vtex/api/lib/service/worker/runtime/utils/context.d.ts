import { Context } from 'koa';
import { IOContext, TracingContext } from '../typings';
type HandlerContext = Omit<IOContext, 'route'>;
export declare const prepareHandlerCtx: (header: Context['request']['header'], tracingContext?: TracingContext) => HandlerContext;
export {};

import { Resource } from '@opentelemetry/resources';
import * as metrics from '../metrics';
import { LogClient, TraceClient } from '../types';
import { LogsClientConfig } from '../types/logs';
import { TracesClientConfig } from '../types/traces';
import { MetricsClientConfig } from '../types/metrics';
import { InstrumentationBase } from '@opentelemetry/instrumentation';
export declare class TelemetryClient {
    private resource;
    private clientName;
    private serviceName;
    private traceClient?;
    private logClient?;
    private metricsClient?;
    constructor(clientName: string, serviceName: string, resource: Resource);
    newLogsClient(config: LogsClientConfig): Promise<LogClient>;
    newTracesClient(config: TracesClientConfig): Promise<TraceClient>;
    newMetricsClient(config: MetricsClientConfig): Promise<metrics.MetricsClient>;
    setInstrumentations(instrumentations: InstrumentationBase[]): void;
}
export interface TelemetryClientOptions {
    envMappings?: {
        envVar: string;
        attributeName: string;
        defaultValue?: string;
    }[];
    customAttributesFile?: string;
    additionalAttrs?: Record<string, string>;
    disableCloudDetect?: boolean;
    disableK8sDetect?: boolean;
    environment?: string;
    region?: string;
    enableVtexProcessors?: boolean;
}
export declare function NewTelemetryClient(clientName: string, serviceName: string, options?: TelemetryClientOptions): Promise<TelemetryClient>;
//# sourceMappingURL=client.d.ts.map
import { TracerProvider } from '@opentelemetry/api';
import { Span as OtelSpan } from '../types/common';
import { Span } from './span';
import { TraceClient, TracesClientConfig, SpanOptions, Context, EnhancedContext } from '../types';
import { CompositePropagator } from '@opentelemetry/core';
export declare class TraceClientImpl implements TraceClient {
    private otelTracer;
    private tracerProvider;
    private exporter;
    private standardPropagator;
    constructor(config: TracesClientConfig, serviceName: string, clientName: string);
    startSpan(name: string, options?: SpanOptions): Span;
    endSpan(span: Span): void;
    getProvider(): TracerProvider;
    getPropagator(): CompositePropagator;
    inject(headers: Record<string, string>, ctx?: Context): void;
    extract(headers: Record<string, string>): EnhancedContext;
    getContext(): Context;
    getActiveSpan(): OtelSpan | undefined;
    shutdown(): Promise<void>;
}
export declare const NewClient: (config: TracesClientConfig, serviceName: string, clientName: string) => TraceClient;
//# sourceMappingURL=client.d.ts.map
import { Context, Attributes, SpanKind, Link } from '@opentelemetry/api';
import { Sampler, SamplingResult } from '@opentelemetry/sdk-trace-base';
import { AccountProvider } from '../../types/traces';
export declare class VtexAccountSampler implements Sampler {
    private provider;
    private readonly accountAttributeKey;
    constructor(provider: AccountProvider);
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes, links: Link[]): SamplingResult;
    private sampleByRate;
    toString(): string;
}
export declare function newVtexAccountSampler(provider: AccountProvider): Sampler;
//# sourceMappingURL=vtex-account.d.ts.map
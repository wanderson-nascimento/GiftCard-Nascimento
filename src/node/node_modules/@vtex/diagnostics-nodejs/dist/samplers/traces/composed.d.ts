import { Context, Attributes, SpanKind, Link } from '@opentelemetry/api';
import { Sampler, SamplingResult } from '@opentelemetry/sdk-trace-base';
import { CompositionMode } from '../../types/traces';
export declare class ComposedSampler implements Sampler {
    private samplers;
    private mode;
    constructor(mode: CompositionMode, samplers: Sampler[]);
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes, links: Link[]): SamplingResult;
    toString(): string;
}
export declare function newComposedSampler(mode: CompositionMode, ...samplers: Sampler[]): Sampler;
//# sourceMappingURL=composed.d.ts.map